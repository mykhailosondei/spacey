# Airbnb-Net Project

To run the frontend:
```
cd airbnb-net/ClientApp
npm start
```

To run the backend:
```
cd airbnb-net
dotnet run
```

Then go to localhost:3000 and reload the page if needed. Run the frontend first because of a weird bug.

There might be an issue with the certificate when using Chrome, try using another browser instead.

